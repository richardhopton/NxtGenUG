<Page x:Class="WPFPresenter._03Slide"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Object Serialization 101" Width="600">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <TextBlock Style="{DynamicResource HeaderText}" Text="What is it?"/>
            <TextBlock Text="The process of converting an object into a sequence of bits which can be used to create an object which is semantically identical to the original one but with a new reference."/>
            <Border Height="10"/>
            <TextBlock Text="It is useful for saving an object out to a storage medium (disk/database/etc.) or transferring an object between processes; even over a network connection."/>
        </StackPanel>
    </Grid>
</Page>
